import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  containerlist: {
    flexGrow: 1,
    gap: 8,
    padding: 16,
  },
});

export default styles;
