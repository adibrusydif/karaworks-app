import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerList: {
    flexGrow: 1,
    gap: 8,
    paddingTop: 16,
    paddingBottom: 64,
  },
});

export default styles;
